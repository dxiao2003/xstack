interface:
  display_name: "Bootstrap Webapp Project"
  short_description: "Scaffold Docker-first apps from current docs."
  default_prompt: "Use $bootstrap-webapp-project to initialize this repo as a new Docker-first web app project using current official framework documentation."
